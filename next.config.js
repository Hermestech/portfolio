/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  reactStrictMode: true,
  images: {
    domains: ['public-content.bitsports.co','images.unsplash.com'],
  },
}

module.exports = nextConfig
